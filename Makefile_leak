
main_test_delete_leak: main_test_delete_leak.o linked_list.o main_test_delete 

linked_list.o: linked_list.c linked_list.h 
	gcc -g -c linked_list.c

clean:
	rm -f *.o
	rm -f main_test_delete_leak main_test_delete 

